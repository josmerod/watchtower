# Task Board

Single source of truth for actionable work on Watchtower. Edited in place by
humans and by the `task-board` skill (which iterates one task at a time).

> **Documentos hermanos:** `docs/ROADMAP.md` (horizonte priorizado + valor) ·
> `docs/TASK_BOARD_ARCHIVE.md` (tareas cerradas + changelog histórico) ·
> `docs/FEEDBACK.md` (cajón de sastre del usuario — triajear cada sesión).

## Conventions

- **One task per row.** Rows sorted: `in-progress` first, then `todo` (by
  priority, oldest first), then `blocked`.
- **Status:** `todo` · `in-progress` · `blocked` · `done`.
- **Priority:** `P0` (blocker / production down) · `P1` (high) · `P2` (medium) ·
  `P3` (nice-to-have).
- **Area:** `etl` · `dashboard` · `api` · `infra` · `quality` · `docs` · `sec`.
- **One task `in-progress` at a time.** Flip to `done` only after verification:
  run the command, cite the output, validate visually in production.
- **ID scheme:** `T-NNN`, zero-padded, never reused.
- Done rows move to `TASK_BOARD_ARCHIVE.md` verbatim (compactación).
- Keep `Notes` terse — long design goes in `docs/` or a commit.

## Board

| ID | Status | Priority | Title | Area | Notes |
| :-- | :-- | :-- | :-- | :-- | :-- |
| T-047 | todo | P3 | Re-evaluate BridgeBench after V3 ships | etl | 2026-08-27: the Elo leaderboard page migrated to client-side rendering (Next.js behind Cloudflare — no `<table>` in HTML, no discoverable API endpoints, only 55KB shell). The ETL keeps last-good data and logs a warning on failure. When "V3 ships soon" lands: re-inspect for a JSON API, or render via the browserless container (BROWSERLESS_ENDPOINT already mounted) if the tab matters enough. |
| T-010 | in-progress | P2 | Reduce `except Exception` overuse (798 occurrences in `src/`) | quality | Broad exception swallowing hides bugs. Audit high-risk call sites (ETL `run()`, API handlers) and narrow to specific exceptions or re-raise. Many are `# noqa: BLE001` in dashboards — leave those, focus on `src/etl` + `src/api`. **Progress 2026-08-10:** narrowed all 16 `except Exception` in `src/api/routers.py` to `(OSError, json.JSONDecodeError, ValueError, TypeError, ValidationError)` — the realistic data-loading failure set; unexpected programming errors now propagate instead of being masked as 500s. API tests green. ETL tier (413 sites, many intentional resilience catches in `BaseETL.run`/retry/circuit-breaker) deferred — narrowing those risks breaking the resilience guarantee and needs per-site judgement; better as dedicated follow-up. |
| T-003 | in-progress | P2 | Work down mypy errors in `src/` (433 across 104 files) | quality | Hotspots: `src/data_quality/deduplication.py`, `src/etl/anime/mal_etl.py`, `src/utils/file_system.py`, `src/utils/logging.py`. Goal: get `uv run mypy src` green so the manual pre-commit hook can be promoted to blocking. **Progress 2026-08-10:** added `[[tool.mypy.overrides]] ignore_missing_imports` for 13 third-party libs lacking stubs (dash_bootstrap_components, feedparser, plotly.*, yt_dlp, psutil, cloudscraper, nltk, pytesseract, sklearn.*) — cleared 109 `[import-untyped]` errors (565→456). Remaining 456 are real per-site type errors (`arg-type` 69, `attr-defined` 68, `assignment` 60) concentrated in spanish_public_aid/arxiv services/deduplication; these need per-site fixes. |
| T-038 | blocked | P3 | Fix Artificial Analysis ETL (API changed, needs paid key) | etl | Renumbered 2026-08-19 from the original T-026 proposal (ID collided with the spec-rollout rows). The AA v2 API requires an API key (401 without). `ARTIFICIAL_ANALYSIS_API_KEY` env var is supported by the ETL but none is configured; some v2 endpoints also 404'd, suggesting further restructuring. To restore: obtain a key from artificialanalysis.ai, set it in `.env`, verify endpoint paths. Blocked on the user providing a key. |
| T-048 | todo | P2 | TR-F2 momentum por tecnología (spec 13) | dashboard | Menciones esta semana vs media móvil 4 semanas → "Subiendo 🔺/Bajando 🔻" bajo el radar plot. **Diferido hasta ~2026-09-16**: los snapshots M5 (kdnuggets + cloud_updates) empezaron el 2026-08-19 y la media de 4 semanas necesita historia. Desbloqueado por T-041/TR-F3 ya mergeado. |
| T-049 | done | P1 | CI: hacer pytest bloqueante (suite verde) | quality | Done 2026-08-27 (commits dbeb09c). **El flag era la punta del iceberg**: el guard de `Tests/e2e/conftest.py` marcaba TODOS los ítems de la sesión como skipped cuando falta pytest-playwright (los hooks de colección ven la lista completa) → el step `pytest Tests/` de CI era un no-op verde (750 skips) invisibilizado además por continue-on-error. Guard estrechado (solo items con fixture `page`, bajo e2e/, o marcados `e2e`). Eso destapó **9 failures reales**, todos resueltos: **bug REAL en `BaseWatcher.check()`** (la rama first-run nunca avanzaba `self.previous_state` → trigger_alarm no podía dispararse JAMÁS — los watchers eran estructuralmente incapaces de alarmar; corregido + test de regresión determinista), 5 aserciones stale de search-integration (esperaban mutación `<mark>` inline de la era pre-batch-C), `test_related_content_ui.py` eliminado (feature removida en T-025), test visual marcado e2e (requiere servidor vivo), MockWatcher hoisted (NameError). `continue-on-error` removido. **Verificación: `uv run pytest Tests/ -q` completo = 701 passed / 53 skipped** (el comando exacto de CI). El run verde de Actions se confirmará en el próximo push. |
| T-050 | done | P1 | Security intelligence — CISA KEV + security news (keyless) | etl | Done 2026-08-27, deployed + visualmente validado (commits 1e00f79 + fix tarjeta ransomware). Área NUEVA: `src/etl/security/security_feeds_etl.py` mergea **4 fuentes keyless verificadas live**: CISA KEV (JSON oficial, 60 CVEs explotados más recientes con vendor/ransomware/acción requerida, enlaces NVD), The Hacker News (50), SecurityWeek (10), Krebs on Security (10) — **BleepingComputer descartado: Cloudflare 403**, documentado. Tab **🛡️ Security** (patrón Markets): cards (60 CVEs activos · 12 añadidos 7d · **4 con ransomware** — contador corregido con campo dedicado `ransomware_use` tras detectar en la inspección visual que contaba 60) + tablas separadas KEV/noticias con badges de severidad. Registrado en orquestador + freshness watcher. **Prod evidence**: 130 items in-container; tab renderiza 60 badges 🔴 Exploitado + 60 links NVD + 70 noticias; cards correctas. 5 unit tests; suite unit 265/5, árbol completo 701/53. |
| T-051 | done | P2 | "Mi stack" release radar (spec 13 TR-F4) | dashboard | Done 2026-08-28, deployed + validado. `src/etl/github/stack_releases_etl.py` (6 feeds keyless; HA core elegido sobre OS/addons con cap 3; feed Tdarr muerto desde 2020, documentado) + tercer tab "🧮 Mi stack" + releases mezclados en el feed unificado Todos con badge de repo. **Prod**: 28 releases in-container (6/6 repos); tab muestra "25 releases · ArchiSteamFarm, Tdarr, core, immich, jellyfin, n8n" cronológico. |
| T-052 | done | P2 | DataFreshnessWatcher → reglas de Notifications | dashboard | Done 2026-08-28, deployed + validado. Hallazgo: AlertEngine es un content-matcher, no un writer — el tab Notifications lee `data/alerts/rules.json` → nuevo `src/alerts/rules_store.py` hace upsert idempotente desde el watcher (stale→medium, critical/missing→high, recuperación borra; ids `data_freshness_{key}` no tocan reglas manuales) + severity badge en el tab. **Prod**: regla "Freshness: BridgeBench Elo sin datos desde 2026-05-21 (2366.8h)" Active+HIGH visible en Notifications; Metrics card intacta. |
| T-053 | done | P2 | ⭐ Guardados en News/Radar/Markets | dashboard | Done 2026-08-28, deployed + validado. Núcleo compartido en `saved_items.py` (registro SAVE_CANDIDATES + builder + factoría de callback): ⭐ en News Global, feed Todos del Radar (454 artículos) y tabla Markets (50 filas), mismo `data/garden/saved_items.json` con campo `tab`; KG refactorizado encima + columna badge de origen en Guardados. **Prod**: toggle interactivo verificado (☆→★→☆); 85 callbacks sin conflictos. |
| T-054 | done | P2 | Digest semanal | dashboard | Done 2026-08-28, deployed + validado. `src/etl/analytics/weekly_digest_etl.py` (ficheros locales, idempotente) compila términos 🔥 + mejores del Radar por menciones + movers + contexto watchers → `data/insights/digest_latest.json`; tab nuevo **📅 Digest** (3 cards + refresh). **Prod**: 9 términos (agent 68·12 fuentes), 10 picks, 3+3 movers, 0 missing inputs. |
| T-055 | done | P3 | Retención de snapshots en data/ | infra | Done 2026-08-28, deployed + aplicado en prod. `src/utils/snapshot_retention.py` (plan pura + apply, dry-run por defecto, `--apply` explícito): agrupa por dir+prefijo, keep-last-30, NUNCA toca `*_latest.*`/watchers/state/sin-timestamp. Extendido a `.csv` tras detectar ~1480 CSVs por fuente. Paso post-pool en el orquestador. **Prod**: podados miles de json (~3 GB+) + ~8k CSVs (venturebeat 1481→62; `_latest` intactos: 209). |
| T-056 | done | P3 | API pública: /api/v1/markets, /radar, /freshness | api | Done 2026-08-28, deployed + verificado desde LAN. Router `/api/v1` (FastAPI ya en 45714): envelopes Pydantic, `?limit≤500`, 404 si falta el fichero, no-store; radar replica el merge del tab sin importar Dash. **Prod**: markets 200 (bitcoin), radar 200 (newest-first, source=wired), freshness 200 (stale_sources:1). Listo para n8n. |
| T-057 | todo | P3 | Búsqueda global unificada cross-tab | dashboard | News y KG tienen búsquedas globales separadas. Unificar en un índice único (título+resumen+fuente) consultable desde la palette Ctrl+K o un subtab global. Medio esfuerzo; valor discoverability. |
| T-058 | todo | P3 | Investigar: enriquecer HF Trending con citas CrossRef | etl | CrossRef REST es keyless: contar citas de los papers HF Trending por DOI → columna "citas" en el subtab 🔥. Solo si el tab demuestra uso. Alternativa bio: bioRxiv/medRxiv RSS si el usuario quiere ángulo bio. |
| T-059 | todo | P2 | Re-audit de las 15 tab-specs: refinar fuentes/features pendientes | dashboard | Del FEEDBACK del usuario (2026-08-28): revisar una a una las specs de `docs/tab-specs/` extrayendo lo diferido/implementable — muchas specs tienen items marcados "diferido con motivo" en PROGRESS.md (ICS Valencia, Videos watch-later YA hecho en T-046, DeepLearning.AI, AA modalidades…) más fuentes mencionadas que nunca se cablearon. Output: por spec, items implementados / a-implementar (nueva tarea) / descartado con motivo. Alimenta la siguiente ola de trabajo. |
| T-060 | done | P3 | Ocultar tab Shortcuts (reversible) | dashboard | Done 2026-08-28, deployed + visualmente validado. Del FEEDBACK: nav entry + renderer quitados (News es ya el primer tab); código, callbacks y palette Ctrl+K intactos — reactivar = 2 líneas. **Prod evidence**: Shortcuts ausente del nav; renderers 16 (tab-shortcuts fuera). |
| T-061 | in-progress | P3 | News: "🔎 Global" al final de los subtabs | dashboard | Del FEEDBACK (2026-08-28). Ahora el subtab activo por defecto es Top Tech; Global queda como fallback al final. |
| T-062 | in-progress | P3 | News: quitar dots de salud de fuentes | dashboard | Del FEEDBACK (2026-08-28): siempre en gris + redundantes con la card 🩺 Source Freshness de Metrics. Fila eliminada; `shared/health.py` sigue disponible (KG conserva la suya). |
| T-063 | done | P3 | Summary superior colapsable | dashboard | Done 2026-08-28, deployed + validado. Botón "Estado del sistema" + dbc.Collapse cerrado por defecto (callback toggle + caret rotatorio); interval 5min intacto. **Prod evidence**: colapsado al cargar; click → se abre con cards y caret rota. |
| T-059 | done | P2 | Re-audit de las 15 tab-specs | dashboard | Done 2026-08-28 → **`docs/tab-specs/RE-AUDIT.md`**: estado por item M/F de las 15 specs (hecho/pendiente/descartado con motivo), fuentes mencionadas-jamás-cableadas re-evaluadas (DeepLearning.AI ahora viable vía browserless; ICS Valencia re-evaluada — no requiere estado de usuario; KG reddit-score imposible tras RSS) y 6 tareas derivadas (T-064..T-069). |
| T-064 | done | P2 | News: items-per-page + toggle dedup en Top Tech | dashboard | Done 2026-08-28, deployed + validado (spec 01 M4+M5). Dropdown 25/50/100/250 (default 50) + pager "Página X de Y · N items"; toggle "mostrar duplicados" (off por defecto) + summary "N ocultos en M grupos" — adopta duplicate_filter/deduplication_utils (antes sin adoptantes) dentro del controller único. **Prod**: "Página 1 de 4 · 160 items"; al elegir 100 → "Página 1 de 2"; hoy 0 duplicados cross-fuente → summary correctamente oculto (semántica: 26 tests). |
| T-065 | done | P3 | Videos: modal previsualización + badge NUEVO | dashboard | Done 2026-08-28, deployed + validado (spec 04 F3+F5). Botón ▶ overlay → dbc.Modal con embed `youtube-nocookie` 16:9 + footer (canal, Ver en YouTube, Cerrar); badge NUEVO violeta + contador toolbar comparando `data-published-at` vs `localStorage.wt_videos_last_visit` (debounced). **Bug de producción encontrado y arreglado en la inspección visual**: el botón Cerrar era dinámico → ReferenceError del renderer (Input con string-id inexistente en layout); movido al shell estático. **Prod**: modal abre/cierra con embed real; toolbar "1 NUEVO"; 0 errores consola. |
| T-066 | done | P2 | CoursesWatcher: nuevos cursos que matchean keywords | etl | Done 2026-08-28, deployed. `src/watchers/courses_watcher.py` (patrón assess-pura + BaseWatcher): identidad por URL, snapshot previo en data/watchers/courses/, re-baseline si un proveedor vuelve, eventos `new_matching_course` "nuevo curso de {proveedor} que matchea keywords {…}". Registrado en orquestador tras los ETLs de cursos. **Prod**: baseline 2964 cursos / 5 proveedores, 0 eventos (correcto en primera ejecución). |
| T-067 | todo | P3 | Scavenging: outputs canónicos sin copias | etl | Spec 07 M6 (re-audit T-059). Los ETLs viajeros/gumroad deben escribir solo su canónico y el tab leer vía config (como News); elimina las copias en `data/scavenging/` y la categoría como directorio. Higiene + consistencia. |
| T-068 | done | P3 | Valencia: export .ics de eventos | dashboard | Done 2026-08-28, deployed. Botón "⬇ .ics (N)" + payload JSON oculto + builder VCALENDAR clientside (RFC 5545: CRLF, folding 75 octetos, escaping, DTSTART al-day/timed, UIDs estables; verificado ejecutando el JS real en node). **En prod queda DORMIDO**: 0/28 eventos tienen `start_date` (el ETL de visitvalencia nunca extrajo fechas → botón oculto por diseño). → **T-072** desbloquea el valor real. |
| T-069 | done | P2 | Investigar: LiveBench API + DeepLearning.AI vía browserless | etl | Done 2026-08-28 — **ambas fuentes resultaron estables** e implementadas. (1) LiveBench: requests plano está fingerprint-bloqueado (3/22 probes), pero el dato es keyless vía browserless → `livebench_etl` (46 modelos, 8 categorías + coste, CSV crudo de provenance) + tab 🧪 LiveBench. (2) DeepLearning.AI: NO necesitó browserless — sitemap determinista (119 cursos) + cards rotatorias enriquecen ~46/run → subtab 🧠 en Learning. **Prod**: 46 modelos release 2026-06-25 renderizados con ranking completo; 119 cursos (46 enriquecidos). |
| T-070 | done | P2 | Radar: nuevas fuentes keyless (Lobsters, Phoronix…) | etl | Done 2026-08-28, deployed + validado. Probes live: **añadidas** Lobsters (lobste.rs/rss), Phoronix (rss.php) y **Unraid forums** (forums.unraid.tv NXDOMAIN; descubierto el RSS de Invision `/rss/1-all-unraid-topics.xml`, 100 items); **descartado** changelog nightly (404 + dominio ya cubierto). MAX_UNIFIED_ITEMS 100→120 (el cap ya ahogaba fuentes). Lobsters ETL endurecido al patrón house. **Prod**: feeds intercalados en Todos (🦞 Engineering, 🐧 Linux/Hardware, 🟠 Self-Hosting, 25+ filas de alta señal). |
| T-071 | done | P2 | "🆕 New AI models" vía OpenRouter (keyless) | etl | Done 2026-08-28, deployed + validado. `openrouter.ai/api/v1/models` keyless y byte-estable (3 probes, 387 modelos). ETL con seen-map diff (`openrouter_seen_models.json`, baseline en 1er run, cap 30 newest-first) + subtab "🆕 New Models" (context, $/Mtok in/out, first_seen). **Prod**: baseline establecido in-container (387/0 new), rerun confirma diff activo; cards 0/387/tracking-since renderizan con nota de baseline. |
| T-072 | todo | P2 | Valencia: extraer fechas de eventos (desbloquea .ics) | etl | Hallazgo T-068: 0/28 eventos tienen `start_date` — el ETL de visitvalencia (y quizá otros) solo saca título+URL → el export .ics y el subtab Upcoming están dormidos. Extraer fechas de la agenda (date_text/start_date en el HTML de visitvalencia o JSON-LD de eventbrite) y el botón ⬇ .ics se activa solo. |

## Changelog

- 2026-08-28 — **Sesión paralelizada extensiva (2 oleadas, 8 subagentes)**: cerrados T-053/T-054/T-055/T-056/T-065/T-068/T-070/T-071 + hallazgos derivados: bug real del modal Videos arreglado en prod (Input dinámico → ReferenceError), retención extendida a .csv tras detectar el crecimiento real (~1480 CSVs/fuente), T-072 creado (fechas de Valencia desbloquean el .ics). Nuevas fuentes: Lobsters, Phoronix, Unraid forums, OpenRouter new-models. Suite completa **934 passed / 54 skipped** (antes 806/53); 15 commits; 3 deploys; todo validado visualmente en producción.

- 2026-08-28 — Iteración paralelizada (5 subagentes): cerrados **T-051/T-052/T-064/T-066/T-069** — 2 fuentes nuevas estables (LiveBench vía browserless con ranking completo; DeepLearning.AI vía sitemap 119 cursos), tab "🧮 Mi stack" en el Radar, CoursesWatcher (baseline 2964 cursos), loop freshness→Notifications (regla BridgeBench HIGH visible) y News paginación+dedup. Suite completa **806 passed / 53 skipped** (antes 701/53); 6 commits; deployed + validado visualmente tab a tab.

(Histórico completo en `docs/TASK_BOARD_ARCHIVE.md`.)

- 2026-08-27 — Compactación del board: 33 filas done movidas íntegras a `docs/TASK_BOARD_ARCHIVE.md`; este fichero queda solo con tareas abiertas. Cerrado T-041 (TR-F3 feed unificado validado en prod: 426 artículos, filtros fuente/categoría OK). Añadidas del brainstorm: T-048 (momentum, diferido a ~09-16), T-049 (CI bloqueante), T-050 (security keyless), T-051 (mi stack), T-052 (watcher→alerts), T-053 (guardados multi-tab), T-054 (digest), T-055 (retención), T-056 (API pública), T-057 (búsqueda global), T-058 (CrossRef, investigar).
- 2026-08-27 — Closed T-049: pytest bloqueante en CI — el guard de playwright saltaba toda la sesión (no-op verde de 750 skips); estrechado, 9 failures ocultos resueltos (1 bug real de BaseWatcher.check que impedía cualquier alarma), árbol completo 701/53 verde con el comando exacto de CI.
- 2026-08-27 — Closed T-050: 🛡️ Security live — CISA KEV (60 CVEs explotados, links NVD) + THN/SecurityWeek/Krebs (70 noticias), tab con cards y badges validado en prod (130 items, 4 ransomware-linked); BleepingComputer descartado (Cloudflare 403). Suite unit 265/5.
- 2026-08-28 — Primer triaje del FEEDBACK del usuario (5 entradas → T-059..T-063). Quick wins T-060..T-063 implementados, desplegados y validados visualmente en un solo deploy: Shortcuts oculto (reversible), 🔎 Global al final (Top Tech activo por defecto), dots de News eliminados, summary superior colapsable. T-059 (re-audit de las 15 specs) añadido.
- 2026-08-28 — Closed T-059: re-audit completo de las 15 specs → docs/tab-specs/RE-AUDIT.md (estado por item M/F, fuentes re-evaluadas, 6 tareas derivadas T-064..T-069 — destaca T-066 CoursesWatcher, desbloqueado por el fix de BaseWatcher.check de T-049).
