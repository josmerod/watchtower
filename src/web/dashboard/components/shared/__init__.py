"""Shared dashboard building blocks (spec 15 M1/M2).

Tabs should compose these helpers instead of duplicating table builders,
TTL caches and refresh buttons. Keep this package dependency-free from
individual tab modules.
"""
